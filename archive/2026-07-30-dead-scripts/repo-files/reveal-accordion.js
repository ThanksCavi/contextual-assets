/* Moved into webflow-scripts/global/global.js, which every page already loads.
   Kept as a no-op instead of deleted: /who-we-are still carries the tag, and a
   second copy of the module would bind the same cards twice. The tag can come
   out of the page whenever convenient. */
